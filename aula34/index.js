// For clássico - Geralmente com interáveis (arrays ou strings)
// For in - Retorna o índice ou chave (string, array, objeto)
// For of - Retorna o valor em si (interáveis, arrays ou strings)

const nome = 'Renan Mendes'

for (let valor of nome) { // Exite um índice de cada vez
    console.log(valor);
}